---
title: "@gwenui/cli"
description: The GwenUI CLI — install blocks into your project with a single command.
---

## Overview

`@gwenui/cli` is the command-line installer for GwenUI blocks. Built with
[Ink](https://github.com/vadimdemedes/ink) — a React-based terminal UI framework.

```bash
gwen-ui init    # setup GwenUI in your project
gwen-ui add <block>  # install a block
```

## Installation

<PackageManagerTabs command="add -g @gwenui/cli" />

## Commands

| Command | Description |
|---|---|
| `gwen-ui init` | Initialize GwenUI in your project |
| `gwen-ui add <block>` | Add a block to your project |

See [CLI docs](/docs/cli/overview) for full reference.

## Version

Current version: `v0.1.0`

Weekly downloads: ~1,604 (via `@jinxsuper/gwen-cli`)

## Status

Published on npm. Stable. ✅
